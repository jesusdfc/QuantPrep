{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://quantprep.me/schema/lesson.schema.json",
  "title": "QuantPrep Lesson front-matter",
  "description": "YAML front-matter of a theory lesson (.mdx). Body is Markdown/MDX + LaTeX.",
  "type": "object",
  "additionalProperties": false,
  "required": ["id", "title", "topic", "subtopic", "order"],
  "properties": {
    "id": {
      "type": "string",
      "pattern": "^[a-z0-9]+(-[a-z0-9]+)*$"
    },
    "title": {
      "type": "string",
      "minLength": 3,
      "maxLength": 160
    },
    "topic": {
      "type": "string",
      "enum": [
        "probability",
        "statistics",
        "machine-learning",
        "portfolio-construction",
        "quantitative-research",
        "sql"
      ]
    },
    "subtopic": {
      "type": "string",
      "pattern": "^[a-z0-9]+(-[a-z0-9]+)*$"
    },
    "order": {
      "type": "integer",
      "minimum": 0
    },
    "summary": {
      "type": "string",
      "maxLength": 400
    },
    "license": {
      "type": "string",
      "description": "License of the lesson body. 'original' for authored text, otherwise the permissive license of the reused source.",
      "enum": ["original", "MIT", "Apache-2.0", "CC-BY-4.0", "CC-BY-SA-4.0", "public-domain"]
    },
    "attribution": {
      "type": "string",
      "description": "Required credit line + source URL when the body reuses permissively-licensed material."
    },
    "prerequisites": {
      "type": "array",
      "items": { "type": "string" }
    },
    "resources": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["name", "url"],
        "properties": {
          "name": { "type": "string" },
          "url": { "type": "string", "format": "uri" }
        }
      }
    }
  }
}
