"""Ingestion helpers.

IMPORTANT: ingestion scripts read from and write ONLY to the gitignored ``data/``
directory. They must never write into ``content/``. Distilling raw material into
original, publishable content is a manual, license-aware step.
"""
